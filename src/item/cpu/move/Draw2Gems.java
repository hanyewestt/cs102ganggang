package item.cpu.move;

import app.*;
import item.*;
import item.cpu.*;
import java.util.*;

public class Draw2Gems extends Move {
    private Gem type;
    private HashMap<Gem, Integer> toRemove;

    public Draw2Gems(CPUPlayer player, Gem type) {
        HashMap<Gem, Integer> newTokens = new HashMap<Gem, Integer>();
        
    }
}