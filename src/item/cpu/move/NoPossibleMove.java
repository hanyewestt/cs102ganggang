package item.cpu.move;

import app.*;
import item.*;
import item.cpu.*;
import util.*;

import java.util.*;

public class NoPossibleMove extends Move {
    public NoPossibleMove(CPUPlayer cpu) {
        super(cpu);
    }

    public void doMove() {
    }
}